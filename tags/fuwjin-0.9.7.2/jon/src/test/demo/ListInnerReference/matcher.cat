alias org.hamcrest.CoreMatchers as Matcher
alias java.lang.Boolean as Boolean
alias java.util.ArrayList as List

<Root> {
  list = List.new()
  List.add(list, "value")
  List.add(list, "hello, reference!")
  List.add(list, "other")
  return Matcher.is(list)
}